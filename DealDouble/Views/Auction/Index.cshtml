
@model List<DealDouble.Entity.Auction>

<div id="listingArea" class="col-md-9">

    <div>
        <button id="createButton" class="btn btn-success">Add Auction</button>
    </div>
    <br />
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Title</th>
                <th>Description</th>
                <th>ActualAmount</th>
                <th>StartingTime</th>
                <th>EndingTime</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var auction in Model)
            {
                <tr>
                    <td>@auction.Title</td>
                    <td>@auction.Description</td>
                    <td>@auction.ActualAmount</td>
                    <td>@auction.StartingTime</td>
                    <td>@auction.EndingTime</td>
                    <td>
                        <button class="editButton btn btn-primary" data-id="@auction.ID">Edit</button>
                        <button class="deleteButton btn btn-danger" data-id="@auction.ID">Delete</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>




    <div id="contentArea">

    </div>

    <script>
        $("#createButton").click(function () {
            $.ajax({
                url: "/Auction/Create",
                method: "get"
            }).done(function (response) {
                debugger;

                $("#contentArea").html(response);
            });
        });


        $(".editButton").click(function () {
            $.ajax({
                url: "/Auction/Edit",
                method: "get",
                data: {
                    ID: $(this).attr("data-id")
                }
            }).done(function (response) {
                debugger;

                $("#contentArea").html(response);
            });
        });


        $(".deleteButton").click(function () {

            debugger;

            var result = confirm("Are you sure you want to delete this Auction!!!");

            debugger;

            if (result) {
                $.ajax({
                    url: "/Auction/Delete",
                    method: "post",
                    data: {
                        ID: $(this).attr("data-id")
                    }
                }).done(function (response) {
                    debugger;

                    $("#listingArea").html(response);
                });
            }

        });
    </script>

</div>





